read TAR filesystem and find all files
spi communication to DAC
how to send data to DAC using the right sample rate?

read CV input and Gate input
loop-enable button and implement looping


use fastest SPI speed (20mhz?)
    then use timer to send out samples 16k times per seconds Use "state" to know which sample to send (or if 0 is needed)
